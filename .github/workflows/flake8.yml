name: flake8

on: [push,pull_request]

jobs:
  flake8:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Flake8 run
        run: sudo python -m script.ci.flake8
