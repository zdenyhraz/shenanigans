name: test-msvc

on: [push,pull_request]
env:
  CI: ON

jobs:
  test:
    runs-on: windows-latest
    strategy:
      matrix:
        include:
          - compiler: msvc
            build_type: Debug

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        token: ${{ secrets.ACCESS_TOKEN }}

    - name: OpenCV cache
      id: opencv-cache
      uses: actions/cache@v4
      with:
        path: ./libs/opencv_install
        key: ${{ runner.os }}-opencv-cache-v1

    - name: ONNX cache
      id: onnxruntime-cache
      uses: actions/cache@v4
      with:
        path: ./libs/onnxruntime
        key: ${{ runner.os }}-onnxruntime-cache-v1

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Build
      run: python -u build.py --ci --compiler ${{ matrix.compiler}} --build_type ${{ matrix.build_type}} --targets shenanigans_test

    - name: Test
      run: python -u test.py
