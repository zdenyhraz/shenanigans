name: cppcheck

on: [push,pull_request]

jobs:
  cppcheck:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v2
        with:
          submodules: true

      - name: cppcheck install
        run: sudo apt-get install -y cppcheck

      - name: cppcheck run
        run: |
          cppcheck --enable=all --suppressions-list=.cppcheck-suppressions --includes-file=.cppcheck-includes --inconclusive --error-exitcode=0 src/ 2> cppcheck-result.txt
          cat cppcheck-result.txt
        #--project=build/compile_commands.json
